import {NgModule} from "@angular/core";
import {ModuleRoutes} from "@anglr/common/router";

import {components} from "./{{name}}.routes";

/**
 * Module for {{name}} pages
 */
@NgModule(
{
    imports:
    [
    ],
    declarations:
    [
    ]
})
@ModuleRoutes(components)
export class {{capitalizedName}}Module
{
}