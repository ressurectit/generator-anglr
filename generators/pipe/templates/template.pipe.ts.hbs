import {Pipe, PipeTransform} from "@angular/core";

/**
 * TODO - write comment
 */
@Pipe(
{
    name: "{{name}}"
})
export class {{capitalizedName}}Pipe implements PipeTransform
{
    //######################### public methods #########################

    /**
     * TODO - write comment, and changes types
     */
    public transform(value: any): any
    {
        return null;
    }
}